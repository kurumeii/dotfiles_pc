[user]
	name = "{{username}}"
	email = "{{email}}"
[credential]
{{#if dotter.packages.linux}}
	helper = store
{{else}}
	helper = manager
{{/if}}
[core]
{{#if dotter.packages.linux}}
	autocrlf = input
{{else}}
	autocrlf = true
{{/if}}
[merge]
	tool = nvim
[diff]
external = difft
