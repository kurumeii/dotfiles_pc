# Taste Preferences
- Uses **dotter** as their dotfile manager; dotfiles repo is at `~/.pc_dotfiles`. Confidence: 0.9
- Prefers symbolic links (managed via dotter's `global.toml`) to keep config files in sync from the dotfiles repo. Confidence: 0.85
- Inside the dotfiles repo, prefers **no dot prefix** on directory/file names (e.g., `commandcode/` not `.commandcode/`), matching the convention used by other entries like `bat`, `nvim`, `opencode`. Confidence: 0.85
- On Windows (paths under `C:\Users\andrew.nguyen1`). Confidence: 0.95
- Prefers removing unused/unnecessary configuration rather than leaving dead entries in place (e.g., stripped a no-op `env` block from an MCP server that doesn't require auth). Confidence: 0.8
- Prefers **hands-on verification** when debugging — expects functional smoke tests (actually calling APIs, running commands) rather than just reading/inspecting config files. Wants independent verification (e.g., curl the API directly) when surface-level errors are ambiguous. Confidence: 0.8
- When investigating compatibility or feature support, expects **multi-source research**: local docs, web search, and upstream project wikis/repos. Confidence: 0.75
- Uses **bun** as their JavaScript/TypeScript runtime. Confidence: 0.85
- When the assistant makes a claim about how a tool or feature works, expects it to **consult official docs/guides before asserting** — won't accept surface-level interpretation of config values without verification against authoritative sources. Confidence: 0.85
- Interested in **cross-tool interoperability** and multi-agent workflows — actively explores whether skills, plugins, and features can be shared or reused across different AI coding tools (e.g., Command Code, Claude Code, OpenCode, Gemini CLI). Confidence: 0.8
- Prefers **tabular output** for comparisons and structured data rather than prose or bullet lists. Confidence: 0.85
- Uses **mise** as their toolchain manager (node, bun, uv, rust) and keeps language toolchains in mise rather than system/package-manager installs, to avoid duplication across managers. Confidence: 0.85
- Prefers **manifest-based, one-shot bulk provisioning** of machines: keeps an exported app manifest in the dotfiles repo (e.g. `scoop-bak.json` from Scoop on Windows) and chose **Homebrew + `brew bundle` (Brewfile)** as the Linux Scoop analog (CLI formulae, GUI casks) so Ubuntu can be set up in one step — dislikes installing packages one-by-one. Confidence: 0.8
- Wants **fresh package versions** — rejected `apt` largely for out-of-date packages and chose Homebrew/Linuxbrew on Ubuntu for current versions. Applies even to system-adjacent packages like shells: when the assistant suggested `sudo apt install fish`, explicitly said "i would want brew to install fish instead" and had it added to the Brewfile. Rule: install via brew, never apt, regardless of package type. Confidence: 0.85
- When porting a toolset across platforms, prefers **dropping tools that lack native package support** rather than mapping each to a substitute — dropped Windows-only tools (autohotkey, sharex, win32yank, mingw, vcredist, …) on Linux, and later dropped macOS-only casks (caprine, dbeaver, zen) instead of accepting flatpak fallback installs. Exception: if the vendor publishes an **official** platform-specific package source, use it — re-added wezterm via the official `wezterm/wezterm-linuxbrew` tap (fully-qualified formula) after rejecting both the macOS-only cask and the flatpak fallback. Rule of thumb: an official vendor tap/formula is acceptable; a flatpak/snap/third-party workaround is not. Accepts per-platform tool divergence and keeps the manifest to a single package manager (brew), no workaround installers. Confidence: 0.85
- Linux environment: **fish** shell, **podman** over docker. Confidence: 0.7
- Approves plans tersely and wants **immediate execution** without further back-and-forth (e.g., "nothing, do it", "install it") — once a plan is accepted, just build it rather than asking more clarifying questions. Confidence: 0.7
- Keeps **fonts (Nerd Fonts) out of the Brewfile/package manifest** — explicitly asked to strip all font casks from the Brewfile; font installation is managed separately from app provisioning. Confidence: 0.7
- Prefers to **run long install/provisioning scripts themselves** and paste the output back for diagnosis/fixes (e.g., ran `bootstrap-linux.sh` personally, then reported the cask warnings), rather than having the agent execute big installs. Exception: for **single package installs**, expects the agent to run the install immediately rather than just editing the manifest and handing back commands to run (said "install it" after being told to run `brew bundle` themselves). Rule of thumb: the agent executes individual installs; the user runs bulk provisioning scripts and any sudo-required commands (installs like `apt install`, `chsh`, removals) since the agent has no TTY for the password. Confidence: 0.75
- Wants **exactly one install source per app**: when duplicates existed across managers (keepassxc via apt PPA, snap, and brew), chose to keep the brew cask install and remove the apt and snap copies — brew is the single source of truth even for GUI apps, and the manifest entry stays. Dislikes multiple competing installs of the same tool. Confidence: 0.8
- Uses **PowerShell (pwsh.exe)** as the default shell on Windows (wezterm's `default_prog`), alongside fish on Linux. Confidence: 0.8
- Works on a **company-managed machine** (domain-joined account) with **no admin/sudo rights** — `sudo`, `chsh`, and `/etc/shells` edits are off the limits ("i am in company so i don't have password for admin"). Expects fixes to be achievable **without privilege**: user-writable files only, and user-level workarounds in place of system changes (e.g., an `exec fish` handoff appended to `~/.bashrc` with a `FISH_PARENT` loop guard instead of `chsh`), with anything truly system-level deferred to IT. Confidence: 0.85
- Runs **Ubuntu with GNOME** as their Linux desktop and customizes it with GNOME Shell extensions (Blur my Shell, Ubuntu Dock/Dash-to-Dock, user themes). Maintains GNOME on multiple machines with different sessions — desktop on **X11** (`XDG_SESSION_TYPE=x11`), laptop on **Wayland** — and uses cross-machine behavior differences as the baseline when reporting problems (e.g., "worked on my laptop under Wayland"). Confidence: 0.75
- When the user states a fact about their own configuration or setup (e.g., "i have already added wezterm app to the whitelist"), they expect it to be **taken at face value and verified against source-of-truth**, not contradicted with "you misconfigured it" based on surface-level reads. Pushed back ("then it simply doesn't work because i have already added…") and was proven right. Verify live state authoritatively before blaming user configuration. Confidence: 0.8
- When checking GNOME Shell extension settings, verify via **raw `dconf read`** (e.g., `dconf read /org/gnome/shell/extensions/blur-my-shell/applications/whitelist`), not `gsettings` — extension schemas can be **relocatable**, so `gsettings` returns defaults and can misreport the real state (blur-my-shell's applications schema did exactly this). `dconf` is the source of truth for dconf-backed settings. Confidence: 0.85
- Debugging constraints on the company Ubuntu box: `journalctl` is **permission-blocked** ("No journal files were opened due to insufficient permissions"), `org.gnome.Shell.Eval` is disabled, and `sudo` is unavailable — so system/user journals and live compositor introspection can't be used; prefer user-readable logs like `~/.xsession-errors` for GNOME Shell JS errors on X11. Don't treat empty journal output as "no errors". Confidence: 0.8
- Likes a **translucent, frosted-blur terminal window background** across platforms: WezTerm is configured with `window_background_opacity = 0.6` plus per-platform backdrop blur (`win32_system_backdrop = "Acrylic"` on Windows, `macos_window_background_blur` on macOS, and wanting `wayland_window_background_blur` on Linux/Wayland) — plain transparency is not enough, they want actual blur behind the terminal. Confidence: 0.8
- Wants wezterm's **tab bar hidden** — showed a screenshot of the fancy tab bar in the titlebar and asked how to hide it via `wezterm.lua`. Fits their clean/minimal terminal aesthetic. Not yet settled between `enable_tab_bar = false` (always hide) and `hide_tab_bar_if_only_one_tab = true` (hide only with a single tab) — offer the choice. Confidence: 0.7
- Expects the assistant to **positively identify what a screenshot or vague reference actually points to before proposing fixes** — dislikes guessing at the most obvious candidate (guessed wezterm's tab bar; user shot it down with "not wezterm's bar !"). Investigate the repo/configs first to establish the true source, then propose a fix. Consistent with their broader verification-over-assumption stance. Confidence: 0.8
- Wants the terminal window to have **no OS title/caption bar** (the min/max/close buttons "that every OS has") — complements the hidden tab bar for a clean, minimal window aesthetic. Environment-specific gotcha: on their GNOME/X11 desktop, mutter ignores partial Motif decoration hints, so wezterm's `window_decorations = "RESIZE"` still yields a full GNOME-drawn title bar; only `"NONE"` fully removes it (applied in both the dotfiles repo and the deployed `~/.config/wezterm/wezterm.lua`). Confidence: 0.8
- When a suggested fix doesn't work, reports it tersely ("tried it") and expects the assistant to **escalate to live root-cause investigation** — comparing deployed vs repo config, checking running processes and real window state (e.g. X11 `_MOTIF_WM_HINTS` via `xprop`) — rather than offering another surface-level config guess. Confidence: 0.7
- Commit message style: follows **Git Conventional Commits v1.0.0** strictly — wants exactly one commit message with nothing else output, multiple changes described as bullet points using **only dash or asterisk** symbols, and explicitly no "fancy visuals or ancient characters" (no decorative/unicode art or emoji). Confidence: 0.8
- Prefers **single-line commit messages** (subject only, no body/bullets) — when the `/commit` output was a subject plus a bullet list of changes, replied "1 liner" and accepted just the Conventional Commits subject. Default to a one-liner commit message; only add a bullet body if explicitly asked. Confidence: 0.75
- Wants commits with **no co-author trailer** (e.g. `Co-authored-by: CommandCodeBot`) — explicitly said "Don't put co authored there" after the agent added one, and the redo committed clean. Never add co-author/Co-authored-by lines to their commits. Confidence: 0.9
- Delegates the **full git commit + push cycle to the agent** — after getting the commit message, said "commt then push it" (terse, typo-laden imperative) expecting the agent to stage, commit, and push to the tracked branch immediately without further confirmation. Git operations are agent-executed, unlike bulk provisioning scripts which the user runs themselves. Confidence: 0.75
- Git/GitHub environment gotcha: `gh` is authenticated as **hoanhxlyn** but the dotfiles `origin` remote points to **kurumeii/dotfiles_pc**, which that account can't write to — pushes fail on auth until the accounts are reconciled (`gh auth login`/`switch` to kurumeii, or add hoanhxlyn as collaborator). Check auth vs remote owner before assuming push will work. Confidence: 0.7
- Keeps **one shared dotfiles repo across Windows and Linux** and prefers **platform-conditional logic inside shared config files** instead of duplicating per-OS config files. Strongly prefers solving this with **Dotter handlebars template variables** (e.g., `default_prog = { "{{shell}}" }` in `wezterm.lua`, with `shell` defined per-OS in `.dotter/global.toml`) over runtime platform checks (e.g., `wez.target_triple:find("windows")`) — when offered the runtime-conditional fix, explicitly asked "can we use dotter syntax" instead. Also favors reusing templating conventions already established in the repo (e.g., `vim.o.shell = "{{shell}}"` in nvim config) rather than inventing new mechanisms. Confidence: 0.8
